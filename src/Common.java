
public class Common {

	
	public void common(){
		/*
		ArrayList<String> queries = new ArrayList<String>();	
		String query = "Pacific Rim** The hero is in the army and goes from America to Vietnam. The hero reunites with a girl he met in school. The hero runs across the country several times.The hero plays ping-pong against Chinese team.";
		String query1 = "John stays in New York and goes to California to find a job.";
		queries.add(query);
		queries.add(query1);
		float[] thresholds = {0.75f, 0.80f , 0.85f};
		for(String q : queries){
			for(float threshold : thresholds){
				ArrayList<EventSimilarityMatchData> eventTable = EventSimilarity.getEventSimilarityTable(q,threshold);
				if(eventTable != null){
					System.out.println("Possible Matches : " + eventTable.size());
					for(EventSimilarityMatchData row : eventTable){
						System.out.println(row.toString());
					}
				}
				else{
					System.out.println("No event match found ");
				}
			}
		}
		*/
		
	}
}
